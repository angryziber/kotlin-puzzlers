package puzzler2

val x: String? = "foo"
print(x == "foo" ? "is foo" : "not a foo") - not Java

// What will it print?
// a) is foo
// b) not a foo
// c) Code does not compile
// d) TypeCastException
